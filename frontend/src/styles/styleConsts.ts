export const SPACING = {
  TINY: 4,
  SMALL: 10,
  MEDIUM: 20,
  LARGE: 36,
  HUGE: 48,
} as const;

export const FONT_SIZES = {
  TINY: 10,
  SMALL: 12,
  MEDIUM: 16,
  LARGE: 24,
  HUGE: 32,
} as const;

// Colors, radii, borders and shadows all live in the theme system now —
// see ./theme.ts (definitions) and ./useTheme.ts (the hook).

export const FORM_MAX_WIDTH = 400;
