node_modules
dist
.env
.env.*
!.env.example
.cli-cache
.deploy-params.json
.deploy-logs/
*.db
*.db-journal
.DS_Store
*.tsbuildinfo
frontend/vite.config.d.ts
frontend/vite.config.js

# Auto-downloaded ONNX model + tokenizer cache for content search
**/models/
.venv/

# YOLO weights are downloaded by ultralytics on first use; no need to commit.
face-server/yolov8n.pt

# Local SQLite backups (manual snapshots) — too large for git.
db_backups/

# Expo / Tamagui generated caches — never commit.
**/.expo/
**/.tamagui/

# Local MVP working data + built web frontend
.mvp/
backend/frontend-dist/

# Orchestrator remembered selections
.orchestrator-prefs.json

# Native pipeline output (DB, images, thumbnails, sidecars)
data/

# Local file:// storage buckets — published output (images/thumbnails/sidecars/db),
# wherever STORAGE_URL points. Generated, never source.
offline-processing/sds/
**/sidecars/
**/thumbnails/
*.sqlite
*.sqlite-journal
*.sqlite-wal
